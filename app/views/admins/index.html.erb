# TODO Hide edit link if not current admin or global admin

<h1>Listing admins</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Global</th>
  </tr>

<% for admin in @admins %>
  <tr class="<%= cycle('shade', '') %>">
    <td><%=h admin.name %></td>
    <td><%=h admin.global %></td>
    <td><%= link_to 'Show', admin %></td>
    <td><%= link_to 'Edit', edit_admin_path(admin) if @current_admin != nil && @current_admin.id != @admin.id && !@current_admin.global || !logged_in? %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New admin', new_admin_path %>
